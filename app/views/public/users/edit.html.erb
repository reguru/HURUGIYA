<div class="top-image mypage" style="height:700px">
  <div style="height:30px"></div>
  <div class="container">
    <div class="text-center p-5 font-type1">
      <h3>プロフィール編集</h3>
    </div>
    <%= form_with model:@user, url: users_mypage_path, method: :patch, multipart: true do |f| %>
      <div class="text-center">
        <% if current_user.profile_image %>
          <%= image_tag current_user.profile_image, class: "img_prev pf-img" %>
        <% else %>

        <% end %>
      </div>

      <div class="d-flex justify-content-center label-content mt-3">
        <label for="user_profile_image">ファイルを選択</label>
        <div class="hidden-content">
          <input class="img_field" id="user_profile_image" type="file" name="user[profile_image]">
        </div>
      </div>

      <div class="row mt-5">
        <div class="col-md-3 col-sm-1"></div>
        <div class="col-md-2 col-sm-4 col-4">
          <%= f.label :"ユーザー名" %>
        </div>
        <div class="col-md-4 col-sm-4 col-4">
          <%= f.text_field :name %>
        </div>
      </div>

      <div class="row mt-3">
        <div class="col-md-3 col-sm-1"></div>
        <div class="col-md-2 col-sm-4 col-4">
          <%= f.label :"メールアドレス" %>
        </div>
        <div class="col-md-4 col-sm-4 col-4">
          <%= f.email_field :email,size: "40x50" %>
        </div>
      </div>

      <div class="row mt-3">
        <div class="col-md-5 col-sm-5 col-4"></div>
        <div class="col-md-4 col-sm-4 col-4">
          <%= f.submit "変更を保存する", class: "btn btn-success" %>
        </div>
      </div>
    <% end %>
  </div>
</div>